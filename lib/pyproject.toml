[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "auth487"
version = "2.0.0"
dependencies = [
    "Authlib>=1.3.0,<2",
    "pymongo>=4.7.2,<5",
    "pyotp>=2.9.0,<3",
    "pytz>=2024.1",
]
requires-python = ">=3.10"
